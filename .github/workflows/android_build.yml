name: Build Android APK with Notifications Fix
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/push-notifications
          npm install

      - name: Create Dummy WWW Dir
        run: mkdir -p www

      - name: Add Android Platform
        run: npx cap add android || true

      - name: Sync Capacitor (The Fix)
        # هذا السطر هو الأهم لأنه ينقل كود الإشعارات لقلب نظام الأندرويد
        run: npx cap sync android

      - name: Copy Firebase Config
        run: cp google-services.json android/app/ || echo "File missing!"

      - name: Build APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Jazmati-Chat-Final-Fix
          path: android/app/build/outputs/apk/debug/app-debug.apk
